@model Section

<a asp-action="Products" asp-route-name="@Model.EnglishName" class="col-sm-6 col-md-4 col-lg-4 col-xl-3">
    <div class="card">
        <div class="card-header">
            <p class="h3 text-center">@Model.Name</p>
        </div>
        <div class="card-body">
            <img class="card-img" src="@Model.GetImageFullLink()">
        </div>
    </div>
</a>
