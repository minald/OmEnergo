@model Section

<form asp-action=@(Model.Id == 0 ? "CreateSection" : "EditSection") method="post">
    <input hidden asp-for="Id">
    <input hidden name="parentSectionId" value="@(Model.ParentSection?.Id)">
    <div class="form-group row">
        <label asp-for="Name" class="col-sm-2 col-form-label text-right"></label>
        <div class="col-sm-5"><input asp-for="Name" class="form-control"></div>
        <label asp-for="SequenceNumber" class="col-sm-2 col-form-label text-right"></label>
        <div class="col-sm-3"><input asp-for="SequenceNumber" class="form-control"></div>
    </div>
    <div class="form-group row">
        <label asp-for="EnglishName" class="col-sm-2 col-form-label text-right"></label>
        <div class="col-sm-10">
            <input asp-for="EnglishName" class="form-control">
            <small class="form-text text-muted">Значение посчитается автоматически, если сделать его пустым</small>
        </div>
    </div>
    <div class="form-group row">
        <label asp-for="ProductProperties" class="col-sm-2 col-form-label text-right"></label>
        <div class="col-sm-10">
            <label class="text-danger">Аккуратно! Удаление или переименование свойств повлечёт удаление соответствующих свойств у продуктов и моделей!</label>
            <textarea asp-for="ProductProperties" rows="3" class="form-control"></textarea>
            <small class="form-text text-muted">Свойства должны быть разделены точками с запятой, например "Длина;Ширина;Высота"</small>
        </div>
    </div>
    <div class="form-group row">
        <label asp-for="ProductModelProperties" class="col-sm-2 col-form-label text-right"></label>
        <div class="col-sm-10">
            <label class="text-danger">Аккуратно! Удаление или переименование свойств повлечёт удаление соответствующих свойств у продуктов и моделей!</label>
            <textarea asp-for="ProductModelProperties" rows="3" class="form-control"></textarea>
            <small class="form-text text-muted">Свойства должны быть разделены точками с запятой, например "Длина;Ширина;Высота"</small>
        </div>
    </div>
    <div class="form-group row">
        <label asp-for="Description" class="col-sm-2 col-form-label text-right"></label>
        <div class="col-sm-10">
            <textarea asp-for="Description" rows="3" class="form-control"></textarea>
        </div>
    </div>
    <div class="text-center">
        <button class="btn btn-primary" type="submit">Сохранить</button>
        @if (Model.IsMainSection())
        {
            <a asp-action="Sections" class="btn btn-secondary">Отмена</a>
        }
        else
        {
            <a asp-action="Section" asp-route-id="@Model.ParentSection.Id" class="btn btn-secondary">Отмена</a>
        }
    </div>
</form>
